var EsConfig = (function() {

    var EsConfig = {
        baseUrl: ES.baseUrl,
        routes: {

            // Datatables
            datatableLanguage: '/assets/admin/DataTable-Macedonian.json',

            // Categories
            categoriesReorder: '/admin/categories/reorder'
        }
    };

    return EsConfig;
})();